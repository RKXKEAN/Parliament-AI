# ─────────────────────────────────────────────────────────────────────────────
# Thai Parliament Transcriber — Requirements
# Install: pip install -r requirements.txt
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
streamlit>=1.35.0

# Speech-to-Text (CPU-optimised Whisper)
faster-whisper>=1.0.0

# LLM
google-generativeai>=0.7.0

# Document export
python-docx>=1.1.0

# Audio processing utilities (optional but recommended)
pydub>=0.25.1          # audio manipulation / format conversion fallback

# ─── System Dependencies (install separately) ────────────────────────────────
# FFmpeg  →  sudo apt install ffmpeg        (Ubuntu/Debian)
#         →  brew install ffmpeg            (macOS)
#         →  winget install Gyan.FFmpeg     (Windows)

# YouTube download support
yt-dlp>=2024.1.0